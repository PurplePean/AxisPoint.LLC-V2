# Logo Assets

Place the following logo files in this directory:

- `Transparent_AXP_LOGO.svg` (preferred for web)
- `Transparent_AXP_LOGO.png` (fallback)

The logo includes "CRE Asset Management" tagline, so no redundant text should be added near the logo in the header or footer.
